plugins:
- id: beer # src
- id: ec2-fleet # src
# this plugin doesn't exist in the CI version 2.387.3.5
# if we didn't use '-A' (plugins for generation only) the script would fail
- id: aws-java-sdk-kinesis
